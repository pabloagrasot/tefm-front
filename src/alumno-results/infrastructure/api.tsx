
import {getUserName} from '../../utils/utils'


const user = getUserName.then

export const alumnoApi = `http://localhost:3500/${user}/students/`
export const imgApi = `http://localhost:3500/`

export const exercisesApi = '/exercises'